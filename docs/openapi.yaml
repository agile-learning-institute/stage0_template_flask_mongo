openapi: 3.0.3
info:
  title: {{info.name}} {{service.name}} API
  description: |
    A Flask + MongoDB API for the {{service.name}} service.
    
    ## Authentication
    All endpoints (except `/dev-login` and `/metrics`) require JWT authentication via the `Authorization` header:
    ```
    Authorization: Bearer <token>
    ```
    
    For local development, obtain a token from the `/dev-login` endpoint.
  version: 0.1.0
  contact:
    name: {{org.name}}
    email: {{org.email}}
  license:
    name: Apache-2.0

servers:
  - url: http://localhost:{{repo.port}}
    description: Local development server

tags:
  - name: Authentication
    description: Development authentication endpoints
  - name: Config
    description: Configuration endpoints
  - name: Metrics
    description: Monitoring endpoints
{% for domain in service.data_domains.controls %}
  - name: {{domain.name}}
    description: {{domain.name}} operations (create, read, update)
{% endfor %}
{% for domain in service.data_domains.creates %}
  - name: {{domain.name}}
    description: {{domain.name}} operations (create, read)
{% endfor %}
{% for domain in service.data_domains.consumes %}
  - name: {{domain.name}}
    description: {{domain.name}} operations (read-only)
{% endfor %}

paths:
  /dev-login:
    post:
      tags:
        - Authentication
      summary: Get development JWT token
      description: Issues a signed JWT for local/development environments
      operationId: devLogin
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                subject:
                  type: string
                  description: User subject/ID
                  default: dev-user-1
                  example: dev-user-1
                roles:
                  type: array
                  items:
                    type: string
                  description: User roles
                  default: ["developer"]
                  example: ["admin", "developer"]
      responses:
        '200':
          description: Successfully issued JWT token
          content:
            application/json:
              schema:
                type: object
                properties:
                  access_token:
                    type: string
                    description: JWT access token
                  token_type:
                    type: string
                    example: bearer
                  expires_at:
                    type: string
                    format: date-time
                    description: Token expiration timestamp
                  subject:
                    type: string
                    description: User subject
                  roles:
                    type: array
                    items:
                      type: string
                    description: User roles

  /api/config:
    get:
      tags:
        - Config
      summary: Get configuration
      description: Returns the current configuration as JSON (requires authentication)
      operationId: getConfig
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Configuration retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  config_items:
                    type: array
                    items:
                      type: object
                  versions:
                    type: array
                    items:
                      type: object
                  enumerators:
                    type: array
                    items:
                      type: object
                  token:
                    type: object
        '401':
          description: Unauthorized - Invalid or missing token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /metrics:
    get:
      tags:
        - Metrics
      summary: Prometheus metrics
      description: Returns Prometheus-formatted metrics
      operationId: getMetrics
      responses:
        '200':
          description: Metrics retrieved successfully
          content:
            text/plain:
              schema:
                type: string

{% for domain in service.data_domains.controls %}
  /api/{{domain.name}}:
    get:
      tags:
        - {{domain.name}}
      summary: Get all control documents
      description: Retrieve infinite scroll batch of sorted, filtered {{domain.name}} documents
      operationId: get{{domain.name}}s
      security:
        - bearerAuth: []
      parameters:
        - name: name
          in: query
          required: false
          description: Optional name filter (supports partial matches, case-insensitive)
          schema:
            type: string
            example: my-control
        - name: after_id
          in: query
          required: false
          description: Cursor for infinite scroll (ID of last item from previous batch, omit for first request)
          schema:
            type: string
            pattern: '^[0-9a-fA-F]{24}$'
            example: 507f1f77bcf86cd799439011
        - name: limit
          in: query
          required: false
          description: Items per page (max 100)
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 10
            example: 10
        - name: sort_by
          in: query
          required: false
          description: Field to sort by (name, description, status, created.at_time, saved.at_time)
          schema:
            type: string
            default: name
            example: name
        - name: order
          in: query
          required: false
          description: Sort order
          schema:
            type: string
            enum: [asc, desc]
            default: asc
            example: asc
      responses:
        '200':
          description: Successfully retrieved controls
          content:
            application/json:
              schema:
                type: object
                required:
                  - items
                  - limit
                  - has_more
                  - next_cursor
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/Control'
                  limit:
                    type: integer
                    example: 10
                  has_more:
                    type: boolean
                    example: true
                  next_cursor:
                    type: string
                    nullable: true
                    pattern: '^[0-9a-fA-F]{24}$'
                    example: 507f1f77bcf86cd799439011
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      tags:
        - {{domain.name}}
      summary: Create a new {{domain.name}} document
      description: Create a new {{domain.name}} document (requires staff or admin role)
      operationId: create{{domain.name}}
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/{{domain.name}}Input'
      responses:
        '201':
          description: {{domain.name}} created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    type: string
                    pattern: '^[0-9a-fA-F]{24}$'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalError'

  /api/{{domain.name}}/{{domain.name}}Id}:
    get:
      tags:
        - {{domain.name}}
      summary: Get {{domain.name}} by {{domain.name}}Id
      description: Retrieve a specific {{domain.name}} document by its {{domain.name}}Id
      operationId: get{{domain.name}}
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Successfully retrieved {{domain.name}}
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/{{domain.name}}'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      tags:
        - {{domain.name}}
      summary: Update {{domain.name}} document
      description: Update a {{domain.name}} document (requires admin role). Cannot update _id, created, or saved fields.
      operationId: update{{domain.name}}
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ObjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/{{domain.name}}Update'
      responses:
        '200':
          description: {{domain.name}} updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/{{domain.name}}'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

{% for domain in service.data_domains.creates %}
  /api/{{domain.name}}:
    get:
      tags:
        - {{domain.name}}
      summary: Get all create documents
      description: Retrieve infinite scroll batch of sorted, filtered {{domain.name}} documents
      operationId: get{{domain.name}}s
      security:
        - bearerAuth: []
      parameters:
        - name: name
          in: query
          required: false
          description: Optional name filter (supports partial matches, case-insensitive)
          schema:
            type: string
            example: my-create
        - name: after_id
          in: query
          required: false
          description: Cursor for infinite scroll (ID of last item from previous batch, omit for first request)
          schema:
            type: string
            pattern: '^[0-9a-fA-F]{24}$'
            example: 507f1f77bcf86cd799439011
        - name: limit
          in: query
          required: false
          description: Items per page (max 100)
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 10
            example: 10
        - name: sort_by
          in: query
          required: false
          description: Field to sort by (name, description, created.at_time)
          schema:
            type: string
            default: name
            example: name
        - name: order
          in: query
          required: false
          description: Sort order
          schema:
            type: string
            enum: [asc, desc]
            default: asc
            example: asc
      responses:
        '200':
          description: Successfully retrieved {{domain.name}}s
          content:
            application/json:
              schema:
                type: object
                required:
                  - items
                  - limit
                  - has_more
                  - next_cursor
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/{{domain.name}}'
                  limit:
                    type: integer
                    example: 10
                  has_more:
                    type: boolean
                    example: true
                  next_cursor:
                    type: string
                    nullable: true
                    pattern: '^[0-9a-fA-F]{24}$'
                    example: 507f1f77bcf86cd799439011
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      tags:
        - {{domain.name}}
      summary: Create a new {{domain.name}} document
      description: Create a new {{domain.name}} document (requires staff or admin role)
      operationId: create{{domain.name}}
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/{{domain.name}}Input'
      responses:
        '201':
          description: {{domain.name}} created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    type: string
                    pattern: '^[0-9a-fA-F]{24}$'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalError'

  /api/{{domain.name}}/{{domain.name}}Id}:
    get:
      tags:
        - {{domain.name}}
      summary: Get {{domain.name}} by {{domain.name}}Id
      description: Retrieve a specific {{domain.name}} document by its {{domain.name}}Id
      operationId: get{{domain.name}}
      security:
        - bearerAuth: []
      parameters:
        - name: {{domain.name}}Id
          in: path
          required: true
          description: The {{domain.name}} ID (MongoDB ObjectId)
          schema:
            type: string
            pattern: '^[0-9a-fA-F]{24}$'
            example: 507f1f77bcf86cd799439011
      responses:
        '200':
          description: Successfully retrieved {{domain.name}}
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/{{domain.name}}'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
{% endfor %}

{% for domain in service.data_domains.consumes %}
  /api/{{domain.name}}:
    get:
      tags:
        - {{domain.name}}
      summary: Get all {{domain.name}} documents
      description: Retrieve infinite scroll batch of sorted, filtered {{domain.name}} documents (read-only)
      operationId: get{{domain.name}}s
      security:
        - bearerAuth: []
      parameters:
        - name: name
          in: query
          required: false
          description: Optional name filter (supports partial matches, case-insensitive)
          schema:
            type: string
            example: my-{{domain.name}}
        - name: after_id
          in: query
          required: false
          description: Cursor for infinite scroll (ID of last item from previous batch, omit for first request)
          schema:
            type: string
            pattern: '^[0-9a-fA-F]{24}$'
            example: 507f1f77bcf86cd799439011
        - name: limit
          in: query
          required: false
          description: Items per page (max 100)
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 10
            example: 10
        - name: sort_by
          in: query
          required: false
          description: Field to sort by (name, description)
          schema:
            type: string
            default: name
            example: name
        - name: order
          in: query
          required: false
          description: Sort order
          schema:
            type: string
            enum: [asc, desc]
            default: asc
            example: asc
      responses:
        '200':
          description: Successfully retrieved {{domain.name}}s
          content:
            application/json:
              schema:
                type: object
                required:
                  - items
                  - limit
                  - has_more
                  - next_cursor
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/{{domain.name}}'
                  limit:
                    type: integer
                    example: 10
                  has_more:
                    type: boolean
                    example: true
                  next_cursor:
                    type: string
                    nullable: true
                    pattern: '^[0-9a-fA-F]{24}$'
                    example: 507f1f77bcf86cd799439011
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
{% endfor %}

{% for domain in service.data_domains.consumes %} 
  /api/{{domain.name}}/{{domain.name}}Id}:
    get:
      tags:
        - {{domain.name}}
      summary: Get {{domain.name}} by {{domain.name}}Id
      description: Retrieve a specific {{domain.name}} document by its {{domain.name}}Id (read-only)
      operationId: get{{domain.name}}
      security:
        - bearerAuth: []
      parameters:
        - name: {{domain.name}}Id
          in: path
          required: true
          description: The {{domain.name}} ID (MongoDB ObjectId)
          schema:
            type: string
            pattern: '^[0-9a-fA-F]{24}$'
            example: 507f1f77bcf86cd799439011
      responses:
        '200':
          description: Successfully retrieved {{domain.name}}
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/{{domain.name}}'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
{% endfor %}

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        JWT token obtained from `/dev-login` endpoint.
        Include the token in the Authorization header: `Bearer <token>`

  parameters:
    ObjectId:
      name: controlId
      in: path
      required: true
      description: MongoDB ObjectId
      schema:
        type: string
        pattern: '^[0-9a-fA-F]{24}$'
        example: 507f1f77bcf86cd799439011

  responses:
    BadRequest:
      description: Bad Request - Invalid request parameters
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Unauthorized:
      description: Unauthorized - Invalid or missing token
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Forbidden:
      description: Forbidden - Insufficient permissions
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    InternalError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  schemas:
    Error:
      type: object
      required:
        - error
      properties:
        error:
          type: string
          description: Error message
          example: Unauthorized

    Breadcrumb:
      type: object
      description: Audit trail information
      additionalProperties: false
      required:
        - from_ip
        - by_user
        - at_time
        - correlation_id
      properties:
        from_ip:
          type: string
          format: ip
          description: HTTP request remote IP address
          example: 192.168.1.1
        at_time:
          type: string
          format: date-time
          description: Timestamp of the action
          example: "2024-01-15T10:00:00Z"
        by_user:
          type: string
          description: User ID who performed the action
          pattern: '^[^\s]{1,40}$'
          example: user-123
        correlation_id:
          type: string
          description: Logging correlation ID
          pattern: '^[^\s]{1,40}$'
          example: corr-abc123

{% for domain in service.data_domains.controls %}
    {{domain.name}}:
      type: object
      description: {{domain.name}} domain document
      additionalProperties: false
      required:
        - _id
        - name
        - created
        - saved
      properties:
        _id:
          type: string
          description: MongoDB document ID
          pattern: '^[0-9a-fA-F]{24}$'
          example: 507f1f77bcf86cd799439011
        name:
          type: string
          description: {{domain.name}} name
          pattern: '^[^\s]{1,40}$'
          example: my-control
        description:
          type: string
          description: {{domain.name}} description
          pattern: '^[^\t\n]{0,255}$'
          example: A sample control document
        status:
          type: string
          description: {{domain.name}} status
          enum:
            - active
            - archived
          example: active
        created:
          $ref: '#/components/schemas/Breadcrumb'
        saved:
          $ref: '#/components/schemas/Breadcrumb'

    {{domain.name}}Input:
      type: object
      description: Input for creating a {{domain.name}} document
      properties:
        name:
          type: string
          description: {{domain.name}} name
          pattern: '^[^\s]{1,40}$'
          example: my-control
        description:
          type: string
          description: {{domain.name}} description
          pattern: '^[^\t\n]{0,255}$'
          example: A sample control document
        status:
          type: string
          description: {{domain.name}} status
          enum:
            - active
            - archived
          example: active

    {{domain.name}}Update:
      type: object
      description: Fields to update in a {{domain.name}} document (cannot include _id, created, saved)
      properties:
        name:
          type: string
          description: Control name
          pattern: '^[^\s]{1,40}$'
        description:
          type: string
          description: Control description
          pattern: '^[^\t\n]{0,255}$'
        status:
          type: string
          description: Control status
          enum:
            - active
            - archived
{% endfor %}
{% for domain in service.data_domains.creates %}
    {{domain.name}}:
      type: object
      description: {{domain.name}} domain document
      additionalProperties: false
      required:
        - _id
        - name
          pattern: '^[^\s]{1,40}$'
          example: my-create
        description:
          type: string
          description: {{domain.name}} description
          pattern: '^[^\t\n]{0,255}$'
          example: A sample {{domain.name}} document
        status:
          type: string
          description: {{domain.name}} status
          example: active
        created:
          $ref: '#/components/schemas/Breadcrumb'

    {{domain.name}}Input:
      type: object
      description: Input for creating a {{domain.name}} document
      properties:
        name:
          type: string
          description: {{domain.name}} name
          pattern: '^[^\s]{1,40}$'
          example: my-create
        description:
          type: string
          description: {{domain.name}} description
          pattern: '^[^\t\n]{0,255}$'
          example: A sample {{domain.name}} document
        status:
          type: string
          description: {{domain.name}} status
          example: active

{% endfor %}
{% for domain in service.data_domains.consumes %}
    {{domain.name}}:
      type: object
      description: {{domain.name}} domain document (read-only)
      additionalProperties: false
      required:
        - _id
        - name
      properties:
        _id:
          type: string
          description: MongoDB document ID
          pattern: '^[0-9a-fA-F]{24}$'
          example: 507f1f77bcf86cd799439011
        name:
          type: string
          description: {{domain.name}} name
          pattern: '^[^\s]{1,40}$'
          example: my-{{domain.name}}
        description:
          type: string
          description: {{domain.name}} description
          pattern: '^[^\t\n]{0,255}$'
          example: A sample {{domain.name}} document
        status:
          type: string
          description: {{domain.name}} status
          example: active
{% endfor %}

    InfiniteScrollResponse:
      type: object
      description: Infinite scroll pagination response
      required:
        - items
        - limit
        - has_more
        - next_cursor
      properties:
        items:
          type: array
          description: Array of resource documents
          items: {}
        limit:
          type: integer
          description: Items per batch
          example: 10
        has_more:
          type: boolean
          description: Whether there are more items available
          example: true
        next_cursor:
          type: string
          nullable: true
          description: ID of last item for next request (null if no more items)
          pattern: '^[0-9a-fA-F]{24}$'
          example: 507f1f77bcf86cd799439011
